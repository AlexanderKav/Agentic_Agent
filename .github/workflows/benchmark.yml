name: Performance Benchmarks

on:
  schedule:
    - cron: '0 2 * * *'  # Run daily at 2 AM
  workflow_dispatch:  # Allow manual trigger

jobs:
  benchmark:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4  # Updated to v4
    
    - name: Set up Python
      uses: actions/setup-python@v5  # Updated to v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt
        pip install pytest pytest-benchmark
    
    - name: Run benchmarks
      run: |
        pytest tests/benchmarks/ --benchmark-only --benchmark-json output.json
    
    - name: Store benchmark results
      uses: actions/upload-artifact@v4  # Updated to v4 (critical fix!)
      with:
        name: benchmark-results
        path: output.json
    
    - name: Compare with previous benchmark
      run: |
        python scripts/compare_benchmarks.py output.json